version: '3.4'
services:
  acs71-demo-share:
    image: alfresco-share-acs71-demo:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../acs71-demo-share-docker/target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
      REPO_HOST: acs71-demo-acs
      REPO_PORT: 8080
    ports:
      - "${share.port}:8080"
      - "${share.debug.port}:8888"
  acs71-demo-acs:
    image: alfresco-content-services-acs71-demo:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../acs71-demo-platform-docker/target
    environment:
      JAVA_TOOL_OPTIONS: "${keystore.settings}"
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - acs71-demo-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - acs71-demo-postgres
  acs71-demo-postgres:
    image: postgres:13.1
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - acs71-demo-db-volume:/var/lib/postgresql/data
  acs71-demo-ass:
    image: alfresco/alfresco-search-services:1.2.0
    environment:
      SOLR_ALFRESCO_HOST: acs71-demo-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: acs71-demo-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
    ports:
      - "8983:8983"
    volumes:
      - acs71-demo-ass-volume:/opt/alfresco-search-services/contentstore
      - acs71-demo-ass-volume:/opt/alfresco-search-services/data
volumes:
  acs71-demo-acs-volume:
    external: true
  acs71-demo-db-volume:
    external: true
  acs71-demo-ass-volume:
    external: true
